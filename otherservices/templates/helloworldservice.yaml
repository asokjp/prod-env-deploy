apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.hello-world.name }}
  labels:
    app: {{ .Values.hello-world.name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
    - port: {{ .Values.helloworldExternalHttpPort }}
      name: http
  selector:
    app: {{ .Values.hello-world.name }}
