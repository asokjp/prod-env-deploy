apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    sidecar.istio.io/status: {{ .Values.istio.status }}
  creationTimestamp: null
  name: {{ .Values.claimant-service.name }}-{{ .Chart.Version }}
spec:
  replicas: 1
  strategy: {}
  template:
    metadata:
      annotations:
        sidecar.istio.io/status: {{ .Values.istio.status }}
      creationTimestamp: null
      labels:
        app: {{ .Values.claimant-service.name }}
        version: {{ .Values.claimant-service.version }}
    spec:
      containers:
      - env:
        - name: SPRING_PROFILES_ACTIVE
          value: {{ .Values.claimant-service.spring.profiles }}
        image: {{ .Values.claimant-service.image.name }}
        imagePullPolicy: {{ .Values.claimant-service.imagePullPolicy }}
        name: {{ .Values.claimant-service.name }}
        ports:
        - containerPort: {{ .Values.claimant-service.port }}
        resources: {}
      {{ include "istioproxy" .}}
      {{ include "istioinitcontainers" .}}
      {{ include "istiovolume" .}}
status: {}
---